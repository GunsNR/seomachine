/**
 * Test stub for the `server-only` package.
 *
 * The real module throws when imported outside a React Server Component,
 * which would break unit tests for server modules. Next.js still enforces the
 * real guard at build time — this only affects vitest.
 */
export {};
